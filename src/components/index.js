//  this is the main file for components import to make sure that all components are imported in one place
//  this is the file that will be imported in the App.js file

import Header from './Header/Header';
import Footer from './Footer/Footer';

export { Header, Footer };
